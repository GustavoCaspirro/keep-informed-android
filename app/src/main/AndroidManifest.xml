<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.com.keep_informed">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".KeepInformedApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".interactors.about.AboutActivity"
            android:launchMode="singleTop"/>
        <activity
            android:name=".interactors.navigation.ui.NavigationActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".interactors.signup.ui.SignUpActivity"
            android:launchMode="singleTop" />
        <activity android:name=".interactors.signin.ui.SignInActivity" />
        <activity android:name=".interactors.splash.ui.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>